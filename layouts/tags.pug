extends page

block content
  section#tags
    each tag in Object.keys(tags).sort()
      li.tag
        a(name=tags[tag].urlSafe)
        span.tag!= tag 
        each page in tags[tag]
          li
            a(href='/'+page.path)!= page.title
